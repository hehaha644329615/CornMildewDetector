# ============================================================
# 玉米霉变检测项目 - 完整依赖
# 覆盖 v1-v2 (Faster RCNN) + v3-v6 (YOLOv8)
# 建议使用 conda 创建两个独立环境：
#   conda create -n frcnn python=3.8
#   conda create -n yolo python=3.10
# ============================================================

# ========== 深度学习框架 ==========
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0          # YOLOv8 训练/导出

# ========== 数据处理 & 增强 ==========
opencv-python>=4.8.0
numpy>=1.24.0
pillow>=10.0.0
albumentations>=1.3.0        # 数据增强（先划分后增强）
tqdm>=4.65.0

# ========== 可视化 & 评估 ==========
matplotlib>=3.7.0
seaborn>=0.12.0
pycocotools>=2.0.6           # COCO mAP 评估

# ========== 模型转换 & 量化 ==========
onnx>=1.14.0                 # ONNX 导出
coremltools>=7.0             # CoreML 导出（仅 macOS）

# ========== 标注 & 数据解析 ==========
lxml>=4.9.0                  # VOC XML 解析
pyyaml>=6.0

# ========== Faster RCNN 专用 (v1-v2, Python 3.8) ==========
# torch==1.13.1              # 旧版兼容
# torchvision==0.14.1        # 旧版兼容

# ========== YOLOv8 专用 (v3-v6, Python 3.10) ==========
# torch>=2.0.0               # 新版
# ultralytics>=8.0.0         # YOLOv8